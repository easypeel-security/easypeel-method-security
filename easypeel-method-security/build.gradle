pluginManager.apply("com.github.johnrengelman.shadow")

dependencies {
    implementation "com.squareup:javapoet:${javapoetVersion}"
    compileOnly "org.springframework.boot:spring-boot-starter-aop:${springAopVersion}"
    implementation "org.springframework:spring-context:${springContextVersion}"
    implementation "com.github.ben-manes.caffeine:caffeine:${caffeineVersion}"
}

shadowJar {
    archiveBaseName.set(project.name)
    archiveVersion.set("${version}")
    configurations = [project.configurations.compileClasspath]
    dependencies {
        exclude(dependency -> {
            return !(dependency.name.contains("org.aspectj") || dependency.name.contains("org.springframework.stereotype") || dependency.name.contains("com.github.ben-manes") || dependency.name.contains("com.squareup"))
        })
    }
}
